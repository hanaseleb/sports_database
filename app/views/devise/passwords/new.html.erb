<h2>パスワードをお忘れですか？</h2>

<%= bootstrap_form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
    <%= render 'partial/devisemsg' %>

  <div class="field">
    <%= f.email_field :email, autofocus: true %>
  </div>

  <div class="actions">
    <%= f.submit 'メール送信' %>
  </div>
<% end %>
<%= render "devise/shared/links" %>
