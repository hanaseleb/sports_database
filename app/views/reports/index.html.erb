<ul class="nav nav-tabs">
  <li role="navigation" class="active"><%= link_to "Report", reports_index_url(current_user.id) %></li>
  <li role="navigation"><%= link_to "Injury", injuries_index_url(current_user.id) %></li>
  <li role="navigation"><%= link_to "Muscle", user_muscle_path(current_user.id) %></li>
</ul>
<h1>ユーザー記録</h1>
<table class="table table-hover table-responsive table-striped">
  <thead class="thead-default">
  <tr>
    <th>ID</th>
    <th>日付</th>
    <th>体重</th>
    <th>疲労度</th>
    <th>睡眠時間</th>
    <th>吐き気</th>
    <th>備考</th>
  </tr>
  </thead>
  <tbody>
  <% @report_list.reverse_order.each do |report| %>
      <tr>
        <td><%= report.user_id %></td>
        <td><%= report.created_at.to_s(:month) %></td>
        <td><%= report.weight %>kg</td>
        <td><%= report.tired %></td>
        <td><%= report.sleep %></td>
        <td><%= report.nausea %></td>
        <td><%= report.other %></td>
        <!--<td><%= link_to 'Edit', edit_report_path(report) %></td>-->
        <!--<td><%= link_to 'Destroy', report, method: :delete, data: { confirm: 'Are you sure?' } %></td>-->
      </tr>
  <% end %>
  </tbody>
</table>
<!--ページネーション-->
<%= paginate @report_list %>


