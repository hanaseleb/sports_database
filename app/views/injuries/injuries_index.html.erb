<ul class="nav nav-tabs">
  <li role="navigation"><%= link_to "Report", reports_index_url(current_user.id) %></li>
  <li role="navigation" class="active"><%= link_to "Injury", injuries_index_url(current_user.id) %></li>
  <li role="navigation"><%= link_to "Muscle", user_muscle_path(current_user.id) %></li>
</ul>
<table class="table table-striped table-responsive table-hover">
<tr>
    <th>登録日</th>
    <th>ユーザーID</th>
    <th>怪我番号</th>
    <th>部位</th>
  </tr>
  <tr>
    <% @injury_list.each do |injury| %>
    <td><%= injury.created_at %></td>
    <td><%= injury.user_id %></td>
    <td><%= injury.id %></td>
    <td><%= injury.part %></td>
  </tr>
<% end %>

    <!--ページネーション-->
    <%= paginate @injury_list %>
